spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nutriai?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 123456
  
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    root: INFO
    com.nutriai: DEBUG
    org.springframework.web: DEBUG
